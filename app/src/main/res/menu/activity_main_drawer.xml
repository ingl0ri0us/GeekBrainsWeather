<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/three_hour_forecast"
            android:icon="@android:drawable/ic_menu_camera"
            android:title="@string/three_hour_forecast" />
        <item
            android:id="@+id/sensor_temperature"
            android:icon="@android:drawable/ic_menu_gallery"
            android:title="@string/sensor_temperature" />
        <item
            android:id="@+id/list_of_cities"
            android:icon="@android:drawable/ic_menu_slideshow"
            android:title="@string/list_of_cities" />
    </group>

    <item android:title="@string/communicate">
        <menu>
            <item
                android:id="@+id/about"
                android:icon="@android:drawable/ic_menu_share"
                android:title="@string/about" />
        </menu>
    </item>

</menu>